# packages/

Shared code used by more than one app (e.g. a future shared TypeScript
types package generated from the API's OpenAPI schema, or a shared Python
package if config/logging duplication between `apps/api` and
`apps/worker` becomes a maintenance problem). Empty at Phase 1 —
nothing is shared yet.
