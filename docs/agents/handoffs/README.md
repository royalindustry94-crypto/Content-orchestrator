# Continuation handoffs

The Build Continuation Agent writes one file here immediately after takeover
and before product edits.

Name: `HANDOFF_<UTC-DATE>_<lane>.md`

Required fields are listed in `docs/agents/HANDOFF_PROTOCOL.md`.
