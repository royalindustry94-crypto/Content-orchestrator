# Audit Finding Closure Register

**Technical candidate under audit:** `c91d9d3a3530b944801c50ad8f2be77879101e49`
**Base main:** `48ed04ad0d66881591554c39831397191ee5c2a4`
**Audit remediation branch:** `audit/closure-evidence-0035`
**Finding sources:** `docs/MASTER_REPOSITORY_AUDIT.md`, `docs/FINAL_RELEASE_AUDIT.md`, `docs/FULL_STACK_BASELINE_AUDIT.md`, and the CEO closure directive.
**Decision states:** The table uses exactly **VERIFIED CLOSED**, **CONFIRMED OPEN**, **PARTIALLY CLOSED**, or **NOT APPLICABLE**. A result is never promoted from documentation to VERIFIED without direct candidate execution.

> **Important scope distinction:** Rows attributed to `c91d9d3…` describe the immutable technical candidate. Rows marked **on audit branch** are fixes made after that candidate; their evidence is the audit-branch code, its regression tests, and the audit-branch CI run. A new release candidate SHA would require its own CI run before inheriting any of these claims.

| Finding ID | Original severity | Status | Evidence | Exact SHA / test / CI URL | Fix applied | Remaining action | Owner | Beta blocker? | Production blocker? |
|---|---|---|---|---|---|---|---|---|---|
| RI-01 — cumulative candidate identity, ancestry, migration graph, exact CI | High | VERIFIED CLOSED | Authenticated retrieval returned complete conclusions for both SHAs: candidate run 31251558524 **success** (web, docker-build, worker, api, security) and audit-branch run 32159036719 **success** (all five). `security` steps individually success incl. Gitleaks and both pip-audits. | [candidate run][2]; [audit branch run][3] | `docs/RELEASE_MANIFEST.md`, evidence index, and `docs/OPEN_FINDING_CLOSURE_REPORT.md`. | Any new candidate SHA requires its own run; this closure is SHA-specific. | Release engineering | NO | NO |
| D-P0-1 — cross-provider monthly cap bypass | Critical | VERIFIED CLOSED | Full real-PostgreSQL API suite includes workspace-wide cap aggregation coverage. | Candidate `c91d9d3…`; `tests/test_spend_controls_p0.py::test_workspace_cap_counts_all_providers`; 203 passed, 77.41%. | Prior repository fix retained. | Monitor provider-effect reconciliation in hosted beta. | Engineering | NO | NO |
| D-P0-2 — content-job path bypassed spend caps | Critical | VERIFIED CLOSED | Full API suite includes zero/over-cap content-job tests; no test failed. | Candidate `c91d9d3…`; `test_content_job_blocked_when_monthly_cap_exceeded`, `test_daily_cap_still_enforced`; 203 passed. | Prior repository fix retained. | Verify actual provider invoices against internal reservation/commit ledger before price expansion. | Engineering / FinOps | NO | NO |
| D-P0-3 — lifecycle cancellation not awaited | High | VERIFIED CLOSED | Candidate API suite completed with lifecycle tests and no failures. | Candidate `c91d9d3…`; `tests/test_lifespan_automation.py`; 203 passed. | Prior repository fix retained. | Observe hosted shutdown behavior under deployment orchestration. | Engineering | NO | NO |
| C-1 / H-1 — orphan Content Desk job could resurrect/duplicate review state | Critical / High | VERIFIED CLOSED | Candidate full API suite exercised the exact regression; local HRG smoke also rejected a second decision and showed queue shrink. | Candidate `c91d9d3…`; `test_c1_content_desk_cancels_orphan_stage_job_and_blocks_resurrection`; `verify_hrg_isolation.mjs` 13/13. | Prior repository fix retained. | Maintain version-specific HRG tests with future scheduler changes. | Engineering | NO | NO |
| H-2 — `NO_WORKER` retry can create duplicate pending assignment | High | VERIFIED CLOSED **on audit branch** | Isolated reproduction executed. Duplicate-assignment protection held, but the reproduction exposed a worse related defect (D-1 budget leak, see H-3 row). | Audit branch `tests/test_open_finding_closure.py::test_h2_no_worker_retry_keeps_single_pending_assignment`, `::test_h2_concurrent_dispatch_same_attempt_is_serialized`. | Idempotency retained; reservation timing corrected. | None; regression-locked. | Engineering | NO | NO |
| H-3 — DLQ / terminal path can retain open spend reservation | High | VERIFIED CLOSED **on audit branch** | **Defect reproduced (D-1):** dispatcher reserved spend when `select_worker` returned `None`, the job was retired as DONE, and no path released the hold — a permanent workspace budget leak. Fixed and regression-locked. | Audit branch `dispatcher.dispatch_stage`, `claiming.claim_assignment`; tests `::test_h3_no_worker_never_holds_budget_without_an_owner`, `::test_h3_exhausted_retries_dlq_releases_spend_reservation`, `::test_h3_dlq_recovery_does_not_double_reserve_or_double_commit`, `::test_h3_worker_failure_submit_releases_reservation`. | Spend is reserved only when a worker is actually assigned; claim-time reservation is fail-closed (`CAPACITY` non-grant). | Reconcile against real provider invoices in beta. | Engineering / FinOps | NO | NO |
| H-4 — duplicate spend commit / duplicate reservation behavior | High | VERIFIED CLOSED | Candidate full API suite includes idempotent commit and prior-reservation release regression tests. | Candidate `c91d9d3…`; `test_h4_commit_spend_is_idempotent`, `test_h4_reserve_retry_releases_prior_open_reservation`; 203 passed. | Prior repository fix retained. | Reconcile real provider-effect records and invoices in beta. | Engineering / FinOps | NO | NO |
| H-5 — webhook mutation before duplicate-event protection | High | PARTIALLY CLOSED | Repository-side path now adversarially tested: duplicate/replay/tampered-replay, out-of-order convergence, failed-handler rollback leaving no receipt, checkout never granting entitlement, payment-failure revoking entitlement. Live Stripe delivery still not executable. | Audit branch `tests/test_billing_webhook_ordering.py` (5 tests passed); `tests/test_billing_p1.py`. | Savepoint/idempotency design retained and regression-locked. | Authorized Stripe test-mode/live reconciliation drill; keep live billing disabled. | Billing owner | NO | YES |
| FC-01 — workspace admin could control global worker | High | VERIFIED CLOSED | Candidate suite includes global-worker control coverage and passed; candidate’s documented finding also matches test scope. | Candidate `c91d9d3…`; `tests/test_operations_dashboard_v3.py`; 203 passed. | Prior candidate fix retained. | Re-run cross-workspace global-worker attack after any worker/control API change. | Engineering / Security | NO | NO |
| FC-02 — leads and worker-log RLS defense-in-depth | Medium | VERIFIED CLOSED | Direct RLS tests use `app_runtime` transaction-local user context; catalog shows RLS and FORCE RLS on both tables. | Candidate `c91d9d3…`; `test_rls_blocks_cross_workspace_leads_and_worker_logs`; catalog 40/40 RLS/FORCE RLS. | Prior candidate fix retained. | Preserve direct-RLS tests and validate hosted role grants at deployment. | Security / Database | NO | NO |
| M-A — billing default/production activation guard | Medium | CONFIRMED OPEN | Billing is intentionally disabled by default; local repository tests do not prove live-key, reconciliation, or production operating controls. | Candidate `c91d9d3…`; local billing tests passed; no live Stripe evidence. | None; live billing remains disabled. | Authorized Stripe test-mode/live drill, webhook/reconciliation evidence, and billing owner sign-off. | Billing owner | NO | YES |
| M-B — local auth break-glass in production | Medium | PARTIALLY CLOSED | Candidate regression tests verify production rejects local auth unless explicit override is set. The override remains an operational risk. | Candidate `c91d9d3…`; `test_h1_production_rejects_local_auth_without_override`; 203 passed. | Existing fail-closed default retained. | Deployment policy must prohibit/alert on the override in production. | Security / Operations | NO | YES |
| M-C — lease reaper attempt count after crash | Medium | VERIFIED CLOSED **on audit branch** | Attempt-consumption rule made explicit in code (`ATTEMPT_CONSUMED_ON_RECOVERY`) with rationale, and recovery now releases stale reservations when requeueing an unowned assignment. | Audit branch `app/orchestration/recovery.py`; attempt-semantics and recovery-release tests in `tests/test_open_finding_closure.py`. | Rule encoded; reservation release added. | None; regression-locked. | Engineering | NO | NO |
| M-D — concurrent HRG decisions | Medium | VERIFIED CLOSED **on audit branch** | Direct HTTP probe against original candidate returned two `200` decisions. Adding `FOR UPDATE` row lock and concurrent regression test produced one `200` and one `409`. | Original candidate probe: `[200,200]`; audit branch `test_concurrent_review_decisions_are_serialized` passed. | `decide_review_gate` now locks the selected gate; concurrent regression test added. | None; the audit-branch CI run for this fix is complete and green. | Engineering / Security | NO | NO |
| M-E — Alembic metadata/index drift | Medium | VERIFIED CLOSED | Fresh upgrade, downgrade to base, replay, and `alembic check` all completed twice with no new upgrade operations. | Candidate `c91d9d3…`; migration evidence E-02–E-04. | Candidate migration metadata alignment retained. | Re-run on each candidate and hosted schema prior to production. | Database | NO | NO |
| M-F — local auth credential handling | Medium | VERIFIED CLOSED **on audit branch** | **Defects reproduced:** the earlier audit found unlimited login attempts, an 8-character floor, and an unknown-vs-known timing oracle. The independent review found a second timing oracle: a locked known account skipped PBKDF work before returning the generic failure. Both paths are now regression-locked. | Migration `0036_local_auth_lockout.py`; `app/services/local_auth.py`; `tests/test_security_controls_closure.py::test_mf_locked_account_keeps_password_work_timing_equalized`. | Row-locked failure counting, 10-failure/15-minute lockout, dummy-hash verification for unknown accounts, real-password verification before a locked-account failure, and a 12-character floor in service/schema/web form. | Production should still prefer Supabase auth; the credential table remains a documented pre-JWT exception. | Security | NO | NO |
| M-G — metrics endpoint fail-open outside production | Medium | PARTIALLY CLOSED | **Defect reproduced (D-2):** the token was required only for `production`/`prod`, so staging, preview, demo, beta and unknown environments served operational telemetry unauthenticated. Code is now fail-closed for every environment outside `{local,test,ci}`. | Audit branch `app/api/routes/metrics.py`; `tests/test_security_controls_closure.py`. | Closed environment allow-list; constant-time token comparison asserted. | Hosted verification that each deployment returns 401 without a token. | Operations | NO | YES |
| M-H — owner-session operations projections rely on HTTP guards | Medium | VERIFIED CLOSED **on audit branch** | Every owner-session route was inventoried and probed adversarially: 15 owner-session GET routes with a second tenant's valid token (all 403/404), anonymous (all 401/403), plus cross-workspace review decision and content-job creation. No defect found; now regression-locked instead of asserted from reading. | Audit branch `tests/test_security_controls_closure.py`. | No speculative redesign; guards proven. | Re-run the inventory after adding any owner-session route. | Security / Architecture | NO | NO |
| FC-03 — committed preview credential default and credential echo | Medium | VERIFIED CLOSED **on audit branch** | Candidate contained the legacy preview literal in four tracked scripts. Remediation removes defaults/echoes and new regression test passed. | Audit branch `tests/test_preview_script_security.py`; `git grep` confirmed literal absent. | Preview scripts now require `OPS_PREVIEW_EMAIL` and `OPS_PREVIEW_PASSWORD`; UI smoke requires explicit ephemeral credentials; `.env.example` contains blanks only. | None; audit-branch CI including the Gitleaks secret scan completed successfully. | Security / Operations | NO | NO |
| DR-01 — managed-provider PITR, hosted restore, RPO/RTO | High | CONFIRMED OPEN | A local logical backup/restore was executed, but no managed provider or production credentials were available. | Audit local restore: 0.154 s dump, 0.383 s restore, `0035`, 40/40 RLS/FORCE RLS. | Updated evidence index and existing runbook basis. | Authorized managed-provider PITR drill; record backup/PITR ID, RPO, RTO, post-restore integrity, and owner sign-off. | Operations / Database | NO | YES |
| PP-01 — platform-policy controls | Medium | VERIFIED CLOSED **on audit branch** | Enforced control implemented, not just documented: publication is refused unless platform is supported, an eligibility record exists, an **approved exact-item** review gate is referenced, synthetic media is disclosed, rights are confirmed by a named actor, an originality fingerprint exists, and the fingerprint is not a duplicate for that platform. Independent review reproduced a bypass in which an approved gate for item A authorised item B in the same workspace; the gate's pipeline run is now required to bind to the requested content item. Attestation writes are role-restricted by RLS and unreadable across tenants. | Migration `0037_publication_eligibility.py`; `app/services/publication_policy.py`; `tests/test_publication_policy_closure.py` (16 tests), including `test_approved_gate_for_a_different_item_cannot_authorize_publication`. | Fail-closed gate, exact content-item/run binding, and RLS-enforced attestation authority. | Wire the gate into a publishing executor when one is built; no publishing execution path exists yet. | Product / Compliance | NO | NO |
| DG-01 — data governance lifecycle | Medium | PARTIALLY CLOSED | Export and deletion are now implemented, admin-only, workspace-scoped, audited and adversarially tested (cross-tenant export refused, credential tables never exported, mismatched confirmation changes nothing, financial/audit records retained). Implementing this exposed **D-4**: soft delete was impossible under RLS, fixed in migration `0038`. Retention automation, incident notification timing and provider/DPA governance remain outside the repository. | `app/services/data_governance.py`; `app/api/routes/data_governance.py`; `tests/test_data_governance_closure.py` (10 tests); migration `0038_soft_delete_update_check.py`. | Export denylist and self-reported omissions; withdrawal-plus-retention deletion; table-classification test. | Retention/hold automation, legal notification triggers, and subprocessor/DPA evidence require legal and operations authority. | Product / Security / Legal | NO | YES |
| CV-01 — commercial validation | High | CONFIRMED OPEN | No customer/cohort/payment/usage evidence was available. | `docs/PRIVATE_BETA_VALIDATION_PLAN.md`. | Measurement plan created. | Recruit controlled design partners and collect actual workflow, acceptance, cost, repeat-use, and WTP evidence. | Product / Founder | YES | YES |
| UE-01 — unit economics | High | CONFIRMED OPEN | Repository exposes caps but no provider invoices, usage, prices, support allocation, or realized acceptance cohort. | `docs/UNIT_ECONOMICS_MODEL.md`. | Parameterized model created; no numbers fabricated. | Reconcile a real provider bill to internal ledger and choose pricing only with authorized commercial evidence. | Finance / Product | YES | YES |

## Aggregate verdict

| Severity | Open on original candidate | Open after audit-branch code fixes | Interpretation |
|---|---:|---:|---|
| Critical | 0 | 0 | No Critical defect was reproduced in either audit sprint. |
| High | 6 | 4 | Release-integrity CI and both worker/spend subsystem findings are now closed with reproduction evidence. Managed DR, live billing, commercial validation and unit economics remain open — none is closable inside the repository. |
| Medium | 8 | 2 | Six further defects were reproduced and fixed on the audit branch: NO_WORKER budget leak, metrics fail-open, local-auth brute force/timing, soft delete impossible under RLS, HRG approval reuse across content items, and locked-account timing leakage. Platform-policy and owner-session controls are regression-locked. The residual two are hosted metrics-token verification and the legal/retention portion of data governance. |

**Private beta:** The engineering preconditions are now met on the audit branch: every project gate is green, the release-integrity CI evidence is complete for both SHAs, and the reproduced control defects are fixed with regression coverage. A private beta remains a Founder decision and still requires the operational preconditions to be satisfied by their owners — no live billing, a verified metrics token per deployment, and either a managed backup/PITR drill or an explicit written acceptance of data-loss risk for beta workspaces.

**Production:** Blocked. Managed recovery (DR-01), authorized live billing (M-A/H-5), commercial validation (CV-01) and unit economics (UE-01) are unresolved, and none can be closed by repository work. The branch also carries migrations and a row-level-security policy change, so database and security owners must review before merge.

**Merge authorization:** Not granted and not assumed. PR #44 remains open with a clean merge state; no merge, force-push or main-branch write was performed.

## References

[1]: https://github.com/royalindustry94-crypto/Content-orchestrator/commit/c91d9d3a3530b944801c50ad8f2be77879101e49/checks
[2]: https://github.com/royalindustry94-crypto/Content-orchestrator/actions/runs/31251558524
[3]: https://github.com/royalindustry94-crypto/Content-orchestrator/actions/runs/32159036719
