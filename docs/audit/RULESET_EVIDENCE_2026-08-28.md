# Main ruleset evidence — 2026-08-28

Verified live repository ruleset: `Protect main` (ruleset id `21731627`).

Evidence captured from the GitHub ruleset API:

- Enforcement: `active`
- Target: default branch
- Deletion blocked
- Non-fast-forward updates blocked
- Pull request required
- Required approving reviews: 1
- Dismiss stale reviews on push: enabled
- Last-push approval: required
- Review thread resolution: required
- Extra approval for unattributed changes: required
- Strict required status checks: enabled
- Bypass actors: none
- Current user bypass: never

Required GitHub Actions checks, all integration id `15368`:

1. `api`
2. `worker`
3. `web`
4. `docker-build`
5. `browser-smoke`
6. `security`

This file records repository enforcement evidence only. It does not by itself certify any application/runtime release candidate.
