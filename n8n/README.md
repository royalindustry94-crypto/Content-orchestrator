# n8n/

Exported n8n workflow JSON (e.g. `script_generation.json`,
`video_assembly.json`) checked in here for version control, plus any
n8n-side setup notes (credential names it expects, webhook URLs it calls
back to). Populated once the first workflow integration is built.
